namespace Library.Presentation;

/// <summary>Stable public type so the host can hand this assembly to <c>MapEndpoints</c> for scanning.</summary>
public sealed class LibraryPresentationAssemblyMarker;
