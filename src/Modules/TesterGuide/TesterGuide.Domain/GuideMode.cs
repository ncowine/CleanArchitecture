namespace TesterGuide.Domain;

/// <summary>Whether a config's tasks are exercised solo or by multiple assigned testers together.</summary>
public enum GuideMode
{
    SinglePlayer,
    Multiplayer,
}
