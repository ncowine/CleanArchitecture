{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_ConnectionStrings_note": "SECRET — do not commit real values here. Override at run time from env vars on the IIS app pool (ConnectionStrings__Students / ConnectionStrings__Library) or a secret store. Empty values below force a fail-fast in Program.cs if the override is missing.",
  "ConnectionStrings": {
    "Students": "",
    "Library": ""
  }
}
