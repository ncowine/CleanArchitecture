# Paste Elasticsearch here

Extract the Elasticsearch **9.x** Windows `.zip` into this folder, so that
`elk/elasticsearch/…/bin/elasticsearch.bat` exists. Download:
<https://www.elastic.co/downloads/elasticsearch>

Then launch everything with `..\start-elk.ps1`. The distribution itself is gitignored.
