# Paste Kibana here

Extract the Kibana **9.x** Windows `.zip` into this folder, so that
`elk/kibana/…/bin/kibana.bat` exists. Download:
<https://www.elastic.co/downloads/kibana>

Then launch everything with `..\start-elk.ps1`. The distribution itself is gitignored.
