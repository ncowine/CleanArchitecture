<UserControl x:Class="CleanArch.DesktopClient.Views.StudentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel Margin="10">
        <Button Content="Back" Command="{Binding BackCommand}" HorizontalAlignment="Left" />
        <TextBlock FontSize="18" Margin="0,10">
            <Run Text="{Binding Detail.FirstName}" /><Run Text=" " /><Run Text="{Binding Detail.LastName}" />
        </TextBlock>
        <TextBlock><Run Text="Email: " /><Run Text="{Binding Detail.Email}" /></TextBlock>
        <TextBlock><Run Text="Status: " /><Run Text="{Binding Detail.Status}" /></TextBlock>
        <TextBlock><Run Text="Active enrollments: " /><Run Text="{Binding Detail.ActiveEnrollments}" /></TextBlock>
        <TextBlock Text="Enrollments" FontWeight="Bold" Margin="0,10,0,0" />
        <ItemsControl ItemsSource="{Binding Detail.Enrollments}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock><Run Text="{Binding Term}" /><Run Text=" — " /><Run Text="{Binding Status}" /></TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="{Binding Error}" Foreground="Red" Margin="0,10,0,0" />
    </StackPanel>
</UserControl>
