<UserControl x:Class="CleanArch.DesktopClient.Views.BookCopiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Back" Command="{Binding BackCommand}" />
            <TextBlock Text="Copies" FontSize="16" Margin="10,0" VerticalAlignment="Center" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Borrow selected for student" Command="{Binding BorrowCommand}" />
                <Button Content="Return selected" Command="{Binding ReturnCommand}" Margin="5,0,0,0" />
                <Button Content="Renew selected" Command="{Binding RenewCommand}" Margin="5,0,0,0" />
                <Button Content="Reserve book for student" Command="{Binding ReserveCommand}" Margin="15,0,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding Notice}" Foreground="DarkGreen" Margin="0,5,0,0" />
            <TextBlock Text="{Binding Error}" Foreground="Red" Margin="0,5,0,0" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Copies}" SelectedItem="{Binding SelectedCopy}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="*" />
                <DataGridTextColumn Header="Condition" Binding="{Binding Condition}" Width="100" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                <DataGridTextColumn Header="Acquired" Binding="{Binding AcquiredOn}" Width="110" />
                <DataGridTextColumn Header="Copy id" Binding="{Binding Id}" Width="260" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
