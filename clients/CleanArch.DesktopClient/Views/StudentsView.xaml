<UserControl x:Class="CleanArch.DesktopClient.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <TextBox Text="{Binding StatusFilter, UpdateSourceTrigger=PropertyChanged}" Width="120" />
            <Button Content="Search" Command="{Binding SearchCommand}" Margin="5,0,0,0" />
            <Button Content="Withdraw" Command="{Binding WithdrawCommand}" Margin="5,0,0,0" />
            <Button Content="Detail" Command="{Binding ViewDetailCommand}" Margin="5,0,0,0" />
            <Button Content="Loans" Command="{Binding ViewLoansCommand}" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Prev" Command="{Binding PreviousPageCommand}" />
            <TextBlock Margin="10,0" VerticalAlignment="Center">
                <Run Text="Page " /><Run Text="{Binding Page}" /><Run Text=" / " /><Run Text="{Binding TotalPages}" />
                <Run Text="  (" /><Run Text="{Binding TotalCount}" /><Run Text=" total)" />
            </TextBlock>
            <Button Content="Next" Command="{Binding NextPageCommand}" />
            <TextBlock Text="{Binding Error}" Foreground="Red" Margin="20,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
