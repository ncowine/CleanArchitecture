<UserControl x:Class="CleanArch.DesktopClient.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Back" Command="{Binding BackCommand}" Margin="0,0,10,0" />
            <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <TextBox Text="{Binding CategoryFilter, UpdateSourceTrigger=PropertyChanged}" Width="120" />
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="10,0,5,0" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" />
            <Button Content="Search" Command="{Binding SearchCommand}" Margin="5,0,0,0" />
            <Button Content="Copies" Command="{Binding ViewCopiesCommand}" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Prev" Command="{Binding PreviousPageCommand}" />
            <TextBlock Margin="10,0" VerticalAlignment="Center">
                <Run Text="Page " /><Run Text="{Binding Page}" /><Run Text=" / " /><Run Text="{Binding TotalPages}" />
                <Run Text="  (" /><Run Text="{Binding TotalCount}" /><Run Text=" total)" />
            </TextBlock>
            <Button Content="Next" Command="{Binding NextPageCommand}" />
            <TextBlock Text="{Binding Error}" Foreground="Red" Margin="20,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                <DataGridTextColumn Header="Available" Binding="{Binding AvailableCopies}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
