<UserControl x:Class="CleanArch.DesktopClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel Width="320" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Sign in" FontSize="20" Margin="0,0,0,10" />
        <TextBlock Text="Actor" />
        <TextBox Text="{Binding Actor, UpdateSourceTrigger=PropertyChanged}" />
        <Button Content="Sign in" Command="{Binding SignInCommand}" Margin="0,10,0,0" />
        <TextBlock Text="{Binding Error}" Foreground="Red" TextWrapping="Wrap" Margin="0,10,0,0" />
    </StackPanel>
</UserControl>
