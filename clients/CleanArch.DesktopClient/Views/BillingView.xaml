<UserControl x:Class="CleanArch.DesktopClient.Views.BillingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Back" Command="{Binding BackCommand}" />
            <TextBlock Text="Account" FontSize="16" Margin="10,0" VerticalAlignment="Center" />
            <TextBlock VerticalAlignment="Center" Margin="10,0,0,0">
                <Run Text="Balance: " FontWeight="Bold" /><Run Text="{Binding Balance, StringFormat=C}" />
            </TextBlock>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Amount:" VerticalAlignment="Center" />
                <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Width="90" Margin="5,0" />
                <TextBlock Text="Description:" VerticalAlignment="Center" Margin="10,0,0,0" />
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="240" Margin="5,0" />
                <TextBlock Text="Category:" VerticalAlignment="Center" Margin="10,0,0,0" />
                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding Category}" Width="110" Margin="5,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Charge" Command="{Binding ChargeCommand}" />
                <Button Content="Record payment" Command="{Binding PaymentCommand}" Margin="5,0,0,0" />
                <Button Content="Waive" Command="{Binding WaiverCommand}" Margin="5,0,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding Notice}" Foreground="DarkGreen" Margin="0,5,0,0" />
            <TextBlock Text="{Binding Error}" Foreground="Red" Margin="0,5,0,0" />
        </StackPanel>
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Prev" Command="{Binding PreviousPageCommand}" />
                <TextBlock Margin="10,0" VerticalAlignment="Center">
                    <Run Text="Page " /><Run Text="{Binding Page}" /><Run Text=" / " /><Run Text="{Binding TotalPages}" />
                    <Run Text="  (" /><Run Text="{Binding TotalCount}" /><Run Text=" entries)" />
                </TextBlock>
                <Button Content="Next" Command="{Binding NextPageCommand}" />
            </StackPanel>
            <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding OccurredOn}" Width="110" />
                    <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="90" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </DockPanel>
</UserControl>
