# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
artifacts/

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# Local SQLite databases (created at runtime / design time)
*.db
*.db-shm
*.db-wal

# Test results & coverage
[Tt]est[Rr]esult*/
*.coverage
*.coveragexml

# OS noise
.DS_Store
Thumbs.db

# Compose secrets — never commit these. Templates live next to them as
# .env.example (observability/dev/, observability/prod/).
**/.env
!**/.env.example

# Locally downloaded Elasticsearch/Kibana distributions. Both environments now
# run ELK from Docker images; this folder is a leftover and can be deleted.
/elk/
