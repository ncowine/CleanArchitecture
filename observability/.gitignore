# Compose secrets (dev/.env, prod/.env). Templates are tracked as .env.example.
.env

# Leftovers from the retired native-Windows stack: downloaded binaries and their
# runtime data, never source. Safe to delete these folders from your working copy.
/tempo/
/loki/
/prometheus/
